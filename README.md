# Codeigniter 3.1.9 HMVC

# GITHUB COMMANDS
1. Create folder "ua" in xampp/htdocs/
2. Download Git
3. Open git in "ua" folder
4. Run Command in git terminal "git clone https://github.com/princejotham/university-of-antique-project.git ./"
It will downlaod the repo in the folder "ua"
5. Set email and name in git repo
git command set email: git config --global user.email "your@email.com"
git command set name: git config --global user.name "yourname"
